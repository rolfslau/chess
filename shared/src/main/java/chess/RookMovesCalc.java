package chess;

public class RookMovesCalc extends PieceMovesCalc {
    public RookMovesCalc(ChessBoard board, ChessPosition pos, ChessGame.TeamColor color) {
        super(board, pos, color);
    }
}
