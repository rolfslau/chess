package chess;

public class KnightMovesCalc extends PieceMovesCalc {
    public KnightMovesCalc(ChessBoard board, ChessPosition pos, ChessGame.TeamColor color) {
        super(board, pos, color);
    }
}
